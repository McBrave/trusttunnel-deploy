# EXAMPLE — the real inventory/hosts.ini is generated by generate-inventory.sh
# (and is git-ignored, since the IP changes and shouldn't be committed stale).
[vpn]
trusttunnel ansible_host=203.0.113.10

[vpn:vars]
ansible_user=ubuntu
